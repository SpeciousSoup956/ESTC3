#include <stdio.h> // Biblioteca com função de entrada e saida

int main()
{
    int vetor [5]; // declara um vetor de 5 posições
    int matriz [5] [3]; //Declara uma matriz de 5 linhas e 3 colunas
    
    //para acessar os elementos do vetor se usa indices.
    //Todos os vetores tem o primeiro elemento na posição 0.
    //Assim de tornamos M o tamanho do vetor, a ultima posição é a M-1.
    
    vetor [0] = 4 // coloca 4 na primeira posição do vetor
    vetor [4] = 8 //coloca 8 na ultima posição do vetor
    
    
}
